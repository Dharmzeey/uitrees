from django.urls import path
from . import views

# app_name = 'trees'
#
# urlpatterns = [
#     path('', views.TreeList.as_view(), name='tree-list'),
#     path('register/', views.RegisterTaxon.as_view(), name='register-taxon'),
# ]